#include "main.h"

/**
 * _memcpy - copies memory area.
 * @s: pointer to char
 * @c: char parameter to found
 * Return: to pointer s
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	while (*s != 0)
	{
		if (*s == c)
		{
			return (s);
		}
		s++;
	}
	if (*s == c)
	{
		return (s);
	}
	return (0);
}
