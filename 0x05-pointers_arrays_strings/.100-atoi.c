#include "main.h"

/**
 * int _atoi - convert a string to an integer
 * @s: string.
 * Return: void.
 */

int _atoi(char *s)
{
	unsigned int count = 0, size = 0, oi = 0, pn = 1, m = 1, i;

	while (*(s + count) != '\0')
	{
		if (size > 0)
	}
}
